name: komodo_dev_tools_flutter
version: 0.0.1
description: Development tools for Flutter projects within Komodo Platform. 
homepage: https://komodoplatform.com/

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # Assets will go here